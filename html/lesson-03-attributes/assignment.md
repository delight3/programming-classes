# Assignment: HTML Attributes

1. Create a paragraph and add an `id` and `class` attribute.
2. Add an image with `src`, `alt`, and `width` attributes.
3. Create a link that opens in a new tab using the `target` attribute.
4. Add an input field with a `placeholder`.
5. Explain the difference between `id` and `class` in your own words.
